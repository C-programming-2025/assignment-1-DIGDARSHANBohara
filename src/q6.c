// Write a C program to calculate the area of a rectangle. Prompt the user to enter the length and width, and display the result.include <stdio.h>
#include <stdio.h>

int main() {
    int Len ;
    int wid ;
    int result ;

    printf ("Enter the length: ");
    scanf("%d", &Len);
    printf ("eneter the width: ");
    scanf ("%d", &wid);
    result= Len * wid ;
    printf("the area is %d\n", result );
    return 0;
}






    

